<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="20dp">

    <!-- The paragraph to type -->
    <androidx.cardview.widget.CardView
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:cardCornerRadius="12dp"
        app:cardElevation="0dp"
        app:cardBackgroundColor="#FFF5EC">

        <TextView
            android:id="@+id/tv_emergency_phrase"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="16dp"
            android:textSize="13sp"
            android:fontFamily="@font/dm_sans"
            android:textColor="#4A3D35"
            android:lineSpacingMultiplier="1.6" />

    </androidx.cardview.widget.CardView>

    <!-- Input field -->
    <com.google.android.material.textfield.TextInputLayout
        style="@style/Widget.MaterialComponents.TextInputLayout.OutlinedBox"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:hint="Type the paragraph exactly…">

        <com.google.android.material.textfield.TextInputEditText
            android:id="@+id/et_emergency_input"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:inputType="textCapSentences|textMultiLine"
            android:minLines="4"
            android:maxLines="8"
            android:textSize="13sp" />
    </com.google.android.material.textfield.TextInputLayout>

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Transcribing this paragraph is an act of intentionality — confirming your exit is deliberate."
        android:textSize="11sp"
        android:fontFamily="@font/dm_sans"
        android:textColor="#8A847C"
        android:layout_marginTop="8dp"
        android:lineSpacingMultiplier="1.4"
        android:gravity="center" />

</LinearLayout>
